services:
  mongodb:
    image: mongo:7
    container_name: ublong-mongodb
    ports:
      - "27017:27017"
    volumes:
      - ublong_mongo_data:/data/db
    restart: unless-stopped

volumes:
  ublong_mongo_data:
